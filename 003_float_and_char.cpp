//===========浮点属与字符练习========
#include<stdio.h>
int main()
{
	//int a, b, c, d;//定义整型变量
	//unsigned int o;//定义无符号变量

	//a = 12;
	//b = -24;
	//o = -100;

	//c = a + o;
	//d = b + o;
	//printf("a + o = %u , b + o = %u	\n", c , d );//使用%u，表示输出的是无符号数(%u为输出十进制无符号数)
	////printf("a + o = %d , b + o = %d \n", c , c );
	////虽然o是无符号数，但是由于输出%d,所以编译器会强制把unsigned改为signed

	//========================第二个练习======================================

	return 0;
}